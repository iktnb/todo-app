# Этап 1 — Foundation и устойчивость

## Цель
Стабилизировать текущую основу приложения перед GTD-расширением: зафиксировать gap относительно `FLOW.md`, добавить надежное локальное хранение и подготовить безопасную площадку для рефакторинга.

## Зависимости
- Нет (стартовый этап).

## Checklist

### data
- [ ] Создать `src/types/storage.ts` с типом snapshot текущего состояния (`columns`, `tasks`, `version`).
- [ ] Добавить константу версии snapshot в `src/constants/storage.ts`.
- [ ] Добавить константу storage key в `src/constants/storage.ts`.
- [ ] Задокументировать в `README.md` формат snapshot и политику версии (когда повышать версию).
- [ ] Добавить в `README.md` краткую таблицу «FLOW entity -> текущий аналог в коде».

### state
- [ ] Добавить в `src/hooks/useBoardState.ts` функцию инициализации state из localStorage.
- [ ] Добавить fallback на дефолтное состояние при невалидном JSON.
- [ ] Добавить fallback на дефолтное состояние при несовместимой версии snapshot.
- [ ] Добавить сохранение `columns` в localStorage при изменениях.
- [ ] Добавить сохранение `tasks` в localStorage при изменениях.
- [ ] Убедиться, что сохранение не ломает DnD сценарий перемещения задач.
- [ ] Убедиться, что сохранение не ломает сценарий удаления задач.

### ui
- [ ] Добавить в `src/components/BoardHeader.tsx` маленький индикатор режима `MVP Foundation` (только текст).
- [ ] Добавить в UI кнопку «Сбросить локальные данные» (в header или рядом), не меняя основную разметку доски.
- [ ] Привязать кнопку к обработчику очистки localStorage + возврату к начальному state.

### validation
- [ ] Проверить, что перезагрузка страницы сохраняет колонки.
- [ ] Проверить, что перезагрузка страницы сохраняет задачи.
- [ ] Проверить, что после ручной порчи JSON в localStorage приложение стартует без падения.
- [ ] Проверить, что при несовместимой версии state корректно сбрасывается.

### manual-test
- [ ] Создать 2 кастомные колонки, перезагрузить страницу, убедиться что колонки на месте.
- [ ] Добавить 3 задачи в Inbox, обновить страницу, убедиться что задачи сохранились.
- [ ] Перетащить задачу в другую колонку, обновить страницу, убедиться что позиция сохранена.
- [ ] Нажать «Сбросить локальные данные», убедиться что остался только Inbox и пустой список задач.

## Definition of Done
- Состояние доски переживает перезагрузку страницы.
- Некорректные данные в localStorage не ломают приложение.
- Есть управляемый способ сбросить локальное состояние.
- Зафиксированы базовые архитектурные ограничения перед GTD-расширением.
