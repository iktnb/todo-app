# Этап 3 — Capture Inbox (MVP)

## Цель
Сделать чистый capture-поток: в Inbox попадает только «сырой» `Item` без преждевременного планирования.

## Зависимости
- Требует завершения `tasks/2.md`.

## Checklist

### data
- [ ] Убедиться, что `Item` создается с `clarified: false` по умолчанию.
- [ ] Добавить поле `source` со значением `manual` для capture из UI.
- [ ] Добавить поле `notes` в модель capture (пока опционально, пустая строка).
- [ ] Добавить ограничение длины `title` (константа в `src/constants/validation.ts`).

### state
- [ ] Добавить в `src/hooks/useBoardState.ts` отдельный handler `handleCaptureItem`.
- [ ] Перевести форму Inbox в `BoardColumn` с `handleAddTask` на `handleCaptureItem`.
- [ ] Добавить trim и блокировку capture при пустом `title`.
- [ ] Добавить защиту от создания дубликата при двойном submit (debounce/guard флагом в handler).
- [ ] Добавить сортировку inbox-элементов по `createdAt` (новые сверху).
- [ ] Добавить удаление raw item (если пользователь решил убрать запись из inbox).

### ui
- [ ] Обновить placeholder в `src/components/BoardColumn.tsx` на capture-ориентированный текст.
- [ ] Добавить microcopy под input: «захватывайте мысль как есть, уточнение позже».
- [ ] Добавить визуальное отличие raw item от clarified item в `src/components/TaskCard.tsx` (бейдж `Inbox`).
- [ ] Скрыть элементы `clarified === true` из сырой inbox-ленты.

### validation
- [ ] Проверить, что пустой ввод не создает item.
- [ ] Проверить, что пробелы по краям `title` обрезаются.
- [ ] Проверить, что после reload raw item остается в Inbox.
- [ ] Проверить, что clarified item больше не показывается в raw Inbox.

### manual-test
- [ ] Ввести короткий item (2-3 слова), убедиться что запись создается мгновенно.
- [ ] Ввести длинный item (около лимита), убедиться что поведение предсказуемо.
- [ ] Нажать Enter дважды быстро, убедиться что не создается дубликат.
- [ ] Добавить 5 item подряд, убедиться в порядке отображения (новые сверху).

## Definition of Done
- Inbox работает как «сырой захват», без принудительной классификации.
- Все новые элементы создаются как `Item` с `clarified: false`.
- Базовые capture-валидации и UX-подсказки добавлены.
- Поведение устойчиво к reload и двойному submit.
